
@{
    ICollection<ShoppersSquare_proto.Models.ProductType> categories = Model;
}

@Html.Partial("_MegaBanner")

@foreach (var category in categories)
{
    @Html.Partial("_CardsContainer", category)
}